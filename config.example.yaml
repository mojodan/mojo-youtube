# YouTube Downloader Configuration
# Copy this file to config.yaml and customize as needed

# Output directory for downloaded videos
output_dir: ./downloads

# Video quality settings
# Options: 'best', '1080p', '720p', '480p', or custom yt-dlp format string
quality: bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best

# Subtitle settings
subtitle_languages:
  - en  # English (primary)
  # - es  # Spanish (uncomment to add more)
  # - fr  # French

subtitle_format: srt  # Options: srt, vtt, json3

# Download behavior
max_retries: 3  # Number of retry attempts for failed downloads
skip_existing: true  # Skip downloading if file already exists

# Authentication
# cookies_file: cookies.txt  # Path to cookies.txt file (Netscape format) for accessing restricted content

# AI Transcription (generates highly accurate English subtitles)
transcribe: false  # Enable Whisper AI transcription after download
whisper_model: base  # Options: tiny, base, small, medium, large (larger = more accurate but slower)
whisper_device: auto  # Options: auto, cpu, cuda (auto detects GPU if available)
whisper_compute_type: default  # Options: default, int8, float16, float32

# File organization
organize_by_channel: false  # Create subdirectories by channel name
download_thumbnail: false  # Download video thumbnail image
save_metadata: true  # Save video metadata as JSON

# Logging and output
verbose: false  # Enable detailed logging
quiet: false  # Suppress all output except errors
log_file: youtube_downloader.log
